# 变更记录

## 1.0.7 — 未发布

### 修掉时间转盘"跟手差、会把整页滑走、个别值很难设"的问题

三个症状其实是**同一个根因**：滚轮和首页在抢同一根手指的竖直拖动。

首页本身是 `verticalScroll`，之前滚轮是 `LazyColumn`（也可竖直滚动），
同轴嵌套滚动会在两者之间分派手势——手指落在滚轮上，竖直拖动却可能被父级接手，
于是"拖时间拖着拖着整页开始动"。整页一动，转盘在屏幕上的位置就漂，
落点跟着漂，**某些值就变得特别难对准**（8 点、12 p.m. 这类恰好在某格边界上的值最明显）。

改成自己接管手势与坐标，两层处理：

1. **转盘不再用 `LazyColumn`。** 位移只有一个浮点状态 `scroll`，单位是"第几项"：
   拖动 1:1 换算，落点由 `round()` 直接得出，跟手且不存在"差一行"的问题。
2. **拖动期间关掉首页的竖直滚动**（`verticalScroll(enabled = ...)`）。
   转盘自己也会消费手势，这是第二道保险。

顺带改的几点：

* `startDragImmediately = true`——手指一落到转盘上就归它管，不再等 touch slop，
  否则首页的滚动会先一步抢走手势。
* 加了**甩动惯性**：快速轻扫按速度折算还会滑多远，不再是"扫一下只挪一格"。
* 行高 38dp → 40dp；离中间越远越淡越小（`graphicsLayer` 在绘制阶段完成，不触发重组）。
* 兜底看门狗：万一某次手势被系统取消、没走到 `onDragStopped`，6 秒后自动恢复首页滚动。

### 为什么之前那版会"静默改错值"

`LazyColumn` 的 `scrollToItem(index)` 与 `contentPadding` 配合决定了某一项究竟落在
顶行还是中行。一旦差一行，转盘**显示的值**和它**回报的值**就不一致——用户没碰它，
时间也可能被改掉。现在坐标是自己算的，不存在这个歧义。


## 1.0.6 — 未发布

### 学习时段改成 iOS 闹钟式的滚轮

原来「开始」「结束」是两个按钮，点开弹系统的时间对话框。现在换成**上下滑动的转盘**：

* 三行可见、中间那行是当前值，手指一拨就滑，停下自动吸附到最近一格。
* 新增**「格式设置」**：12 小时制 / 24 小时制。**没选过时跟随系统设置**
  （设备是 12 小时制就显示 12 小时制），选过之后固定为你的选择。
* 12 小时制写作 `7:00 a.m.`、`11:30 p.m.`，与 iOS 一致；24 小时制写作 `07:00`、`23:00`。
* 时段文字按所选格式实时显示，例如 `7:00 a.m. – 11:00 p.m.`。
* **默认时段改为 7:00 – 23:00**（原为 20:00 – 22:00）。
* 学习时段的开关、卡片标题、位置都没动，只换了时间的设置方式。

滚动只在**停下来之后**才写一次设置，不是每滑过一格就写一次 DataStore。

### 顺手堵掉一个会静默改错时间的隐患

切 12/24 小时制时，小时列的项目数会在 24 项和 12 项之间变化，而 `LazyList` 会保留旧的
滚动下标：轻则显示错位，重则下标落到最后一格，**把时间悄悄改成别的值**。
现在整组滚轮用 `key(use24Hour)` 挂在当前格式上，切格式时重建。

### 记录页的「拦回」不再带后缀

原来首页写「拦回」、记录页写「拦回（选择退出）」，是同一个指标两种叫法。统一为「拦回」。

### 服务没运行起来时自动关掉总开关

总开关开着、`MonitorService` 却不在跑（权限不全、被系统清掉、后台启动被拦），
界面会一直显示"已开启"但什么都不做——这比不开更糟，因为用户以为在监测。
现在打开开关后给 8 秒启动宽限期，仍未起来就自动把总开关关上，并在首页说明原因。

> 代价要说清楚：如果是系统临时把服务清掉了，这种情况不会自动重启服务，而是关开关。
> 这是刻意选的——宁可明确告诉用户"现在没在监测"，也不要留一个看起来在跑、实际不工作的开关。


## 1.0.5 — 未发布

### 修掉一个会让仪表盘"看起来没坏、其实什么都没记"的统计漏洞

朋友连续两次在游戏里被拦回来，首页却一直显示「提醒 0 / 拦回 0 / 监测 0」。查下来是三处叠加：

| # | 问题 | 原因 |
| --- | --- | --- |
| 1 | 游戏确认卡**不计入提醒数** | `ShowGameConfirm` 只弹卡，没动 `nudges` 计数 |
| 2 | 游戏前放弃**不计入拦回数** | 只写了 `gameDeclined`，没写 `returned` |
| 3 | **不足 60 秒的停留被整段丢掉** | `flushTrackedTime()` 只在累计满 1 分钟时落盘，会话结束时直接把余数清零 |

三处合起来的效果就是：只在游戏里活动的那一天，仪表盘三个数字**全都是 0**。

现在：游戏确认卡算一次提醒；点「算了，退出」同时计入拦回；会话结束时余数也落盘（不足 1 秒的零头
才丢弃）。另外「记录」页补了「其中：游戏前同意进入」——原来只有"放弃"这一半，另一半无从查看。

### 首页数据刷新更抗造

`StatsStore.days` 原来用 `stateIn(scope, Eagerly, emptyMap())`。DataStore 的 data 流一旦抛异常
（读写失败、文件损坏），上游就被结束掉，而 StateFlow 只会**冻结在最后一个值**上——不报错、不重试，
界面从此不再更新。改成自己接管收集循环，断了 1 秒后重连。

### 首页新增"一个应用都没选"的提示

总开关开着但一个应用都没勾时，仪表盘会出现三个 0，看起来像坏了。现在这种情况会直接提示
去「监控哪些应用」勾选。

### 引导页支持左右滑动

原来只有「下一步」按钮——很多人第一反应是往左划，划不动就以为卡死。现在三页用
`HorizontalPager` 承载，滑动、按钮、指示点（可点击跳转）三条路都能走。

### 文案修正

* **「也不会替你把应用关掉」是错的。** 应用里就有「退出后清掉该应用的后台进程」这个开关
  （Android 13 及以下有效，默认关闭）。原文案与自己的功能自相矛盾，已改写为准确表述。
* 「记录」页说数据"只写在本机数据库里"——本项目没有数据库，是一个 DataStore 文件。
* 权限页在**只差可选项**时也会显示"四项权限都已就绪"，会当场被用户拆穿。现在分必需/可选两档说明。
* 分类页空列表的提示是一句读不通的话（"已授予使用情况访问之外的应用可见性"），已重写。
* 「退出后清掉该应用的后台」的说明从"纯属顺手"改成说清它到底做什么、什么时候不可用。
* 首页"已暂停"这个状态其实是"服务没起来"，已区分开。
* 首页三张统计卡的趋势文案原来硬写在代码里，而 `strings.xml` 里同样的三条一直没人用——
  两处会各自漂移。现在统一走资源文件。
* 旧品牌名「专注守门」在诊断导出标题和通道提示里还有残留，已改为「知止」。

### 权限页同一条信息两种引号

UI 元素名统一用「」，引语才用 “”。原来同一项设置在一处写「后台弹出界面」、另一处写“后台弹出界面”。

### 工程整理

* 新增 `androidx.compose.foundation:foundation` 显式依赖（别依赖 material3 的传递依赖）。
* 界面文案全部收敛进 `strings.xml`：`HomeScreen` / `StatsScreen` / `DiagnosticsScreen` /
  `PermissionsScreen` / `CategoryScreen` / `MainActivity` / `ZhiZhi.kt` 里的硬编码中文串清零。


## 1.0.4 — 未发布

### 图标：占比从 74% 改成 58%（用真机截图反推的）

之前 74% 是错的。真机装上之后截图实测：

| 实测 | 值 |
| --- | --- |
| 可见图标 | 386×385（正方形 → 遮罩是圆角方形） |
| 字形 / 可见区 | **96.9%**，左右上下各只剩 1.6% —— 所以看起来"文字显示不全" |
| 反推遮罩只露出图层 | **76.4%** |
| → 要让可见区回到原 logo 的 75.4% | **图层占比 = 57.6%** |

现在设成 **58%**：字形占图层 57.9%，除以露出率 76.4% = **占可见区 75.7%**，和原 logo 的 75.4% 一致；
圆形遮罩下也留有余量（官方建议"内容放在中央 66dp"= 61%，58% 在安全区内）。

**教训**：算占比不能拿"整张图层"当参照。adaptive icon 的遮罩只露出图层的约 76%，
按图层算好的比例落到屏幕上会被放大 1.3 倍。

### 预设名单扩到 296 条，删掉「不影响学习」分类

按提供的 300 条清单录入，去掉 4 个 Android 系统应用（计算器/时钟/日历/文件管理，它们本来就不可能被监控），
并按下面的规则处理冲突：

| 冲突包名 | 处理 |
| --- | --- |
| `com.zhihu.android` | 第一类那条其实是"知乎盐选"（同一包名），按第二类算 → **娱乐** |
| `com.ss.android.lark` | 飞书是办公工具 → **不用监测** |
| `com.youdao.dict` / `com.tide.calm` | 同类重复行，各留一条 |
| `com.tencent.tmgp.cf` | 重复行，留一条 |
| `com.tencent.tmgp.lz` | 龙族幻想 / 量子特攻 撞了同一个包名，保留龙族幻想 |

最终：**不用监测 93 条 / 娱乐 103 条 / 游戏 100 条 = 296 条**。
另外补了清单里缺但国内常见的：原神国内版 `com.miHoYo.Yuanshen`（清单里只有国际版，
而真机日志显示国内装的是 Yuanshen）、抖音极速版、芒果TV、斗鱼、剪映、豆瓣、绝区零、Roblox。

`AppCategory.NEUTRAL`（不影响学习）**整个删掉**：预设里"不用监测"那 93 条直接写 `IGNORED`，
分类页回到三态（不监控 / 娱乐 / 游戏）。

### 权限页：每一项都给出当前机型的点法

新增 `ui/screens/OemGuides.kt`，覆盖小米/红米/POCO、华为/荣耀、OPPO/一加/realme、
vivo/iQOO、三星、原生 Android 六类，每类 × 四项权限各一条具体路径。

* 页面顶部标出识别到的机型，默认**只显示这一家的步骤**，一键可展开全部机型
* 特别标出只有国产 ROM 才需要的额外开关（小米的「后台弹出界面」、OPPO 的三个后台开关、
  华为关掉「自动管理」后要手动开三个子开关等）
* 顶部另加一句：路径会随版本变，找不到就在设置里搜「知止」——比背路径管用

### CI：修掉"Resource not accessible by integration"

**根因在 workflow 文件里，不在仓库设置。** 文件顶部写了 `permissions: contents: read`，
而 workflow 里显式声明的 permissions 会**覆盖**仓库的默认设置，
所以"把仓库设置改成 Read and write"这一步单独做是不生效的。

现在拆成两个 job：

* `build`（`contents: read`）：编译 + 两条守门检查 + 上传 Artifacts
* `release`（`contents: write`，只在打 tag 时跑）：下载 Artifacts 并挂到 Release

### CI：新增"不许发 debug 签名包"的硬护栏

没配 `KEYSTORE_BASE64` 时，CI 构建出的 release 包会回落到 **debug 签名**。
这种包**绝对不能发给用户**——用户日后无法用正式密钥覆盖升级。
所以：

* 构建阶段：如果配了密钥，就校验签名证书里不能出现 `CN=Android Debug`
* 发布阶段：`has_key != true` 直接让 job 失败，并把原因写进日志

### 其他

* 版本号 1.0.4（5）。
* 顺带修了 `PresetCatalog` 里 `AppCategory` 限定名缺失导致的编译错误。


## 1.0.2 — 未发布

### 品牌资源：换上新 logo，并且是"拆开用"的

新的 logo 来自「Word 手绘 → 打印 PDF → 在线转 PNG」。这条链路本身能用，但对 Android
图标有三个真会出问题的点，所以**没有直接拿那张 PNG 当图标**：

| 那条链路的问题 | 后果 | 处理 |
| --- | --- | --- |
| 栅格化，分辨率固定 | 启动器要 108dp 渲染，放大发虚 | 采样出渐变做成**矢量背景** |
| 没有透明通道 | 自带白底 + 圆角，系统再套遮罩会在圆里露出白角 | 把白色笔画**抠成透明前景** |
| 背景与前景糊在一起 | adaptive icon 需要分层才能做视差/动画 | 拆成 background + foreground 两层 |

具体做法：
* 从原图**采样**出对角渐变色（左上 `#56AEE3` → 中 `#9E97E8` → 右下 `#D473F0`），
  写成 `ic_launcher_background.xml`（VectorDrawable 渐变）。
* 用"白度"当 alpha 抠出白色笔画，输出 432×432 的透明 PNG 作为前景，
  并按 Android 规范把笔画宽度控制在图层的 **66%**（安全区以内，圆形遮罩也不会切到）。
* `monochrome` 用同一份前景，支持 Android 13+ 主题图标。
* **通知栏小图标改成手绘的嫩芽剪影**——24dp 的单色剪影下，"知止"两个字会糊成一团，
  只用图形部分才看得清。
* 商店列表图标（fastlane）另出一张 512 全幅渐变图。

### 预设名单扩到 78 条，并新增一个分类

按提供的清单录入，分成三类。为了容纳"不影响学习"这一类，
`AppCategory` 新增 **`NEUTRAL`**：行为上和不监控一样（不计时、不提醒），
但语义上是**一次决定**而不是"还没想过"——否则套用预设之后，这些应用和从没勾过的应用长得一模一样，
看不出预设起了作用。

* `NEUTRAL` 不影响学习（20 条）：词典、网课、AI 助手、办公、以及**微信 / QQ**
  （默认认为是在办正事，不是在刷）
* `ENTERTAINMENT` 娱乐 / 社交（38 条）
* `GAME` 游戏（20 条）：含 `com.miHoYo.Yuanshen` 与 `com.miHoYo.GenshinImpact` 两个原神包名

分类筛选同时从"不监控 / 娱乐 / 游戏"改成四态。

### 界面细节

* 首页统计卡**强制等高**（`IntrinsicSize.Min` + `fillMaxHeight`），并删掉"今天开始记录"
  这个占位文案——它只出现在前两张卡上，正是三张卡一高一矮的原因。监测卡也补上了趋势。
* 诊断卡的说明文字从"提醒没弹出来时先来这里看"缩成「反馈问题时用」。
* 「关于」页换成两个收款码（微信赞赏 + 支付宝），删掉所有"发布前请替换…"的提示，
  源码地址改为 <https://github.com/Luo-chiyun/zhizhi-android>。

### 逻辑检查：找到并修掉 4 处

1. **「休息 N 分钟」按了没反应。** 常驻通知的刷新用 key 去重，而 key 里**没有"是否在休息"**，
   所以点完按钮 key 没变 → 直接 return → 通知里的文案和按钮不更新。
2. **统计页"近 7 天"取的是最近 7 条记录，不是最近 7 天。** 中间有几天没记录时，
   会把三周前的数据算进来。改成按日期区间过滤。
3. **子页面不抗横竖屏切换。** 返回栈用 `remember`，Activity 重建就回到首页。
   改成 `rememberSaveable` + `listSaver`。
4. **套用预设会盖掉「别再提醒这个应用」的决定。** 一键套用会把用户明确设为"不监控"的应用
   重新变成娱乐类。现在 `IGNORED` 会被**显式存下来**（区别于"从未决定"），
   套用时跳过这些应用；分类页也加了一句说明让这个行为可见。

### 另外

* 版本号 1.0.2（3）。


## 1.0.1 — 未发布

真机首次上手后的六条反馈，逐条改完：

### 1. 首页三列统计卡会挤（真机上"1 小时 4 分"折成了两行）

统计卡只有三分之一屏宽，而原来的 `formatDuration` 输出"1 小时 4 分"（7 个字符含空格）。
新增 `formatDurationTiny`：最多 4 个字符（`1时4分` / `12分` / `42秒`），
并且统计卡的数值**强制单行**（`maxLines = 1` + `softWrap = false`），
字号从 22sp 收到 19sp。三张卡现在一定等高。

### 2. 顶部被状态栏压住 / 底部被手势条挡住

根因是 **Android 15 起 targetSdk 35 强制 edge-to-edge**，内容会画到状态栏和手势条下面。
现在：内容区 `windowInsetsPadding(statusBars)`；底部 tab 栏用
`windowInsetsPadding(navigationBars)`——白底仍铺到屏幕最底，但图标和文字抬到手势条之上。
子页面（没有 tab 栏）自己避开底部 inset，且**不会重复留白**。

### 3. 拖滑杆时数值不实时，且不能直接输入

`SliderRow` 两个改动：
* 显示的数值取自**本地状态**而不是已保存的设置——拖动过程中数字跟着手指走。
  之前只有松手后才更新，拖动时数字是死的。
* 数值做成可点的胶囊，点一下弹键盘直接输入（带范围提示和单位），
  拖不精确的场合不用再跟滑杆较劲。

### 4. 学习时段默认关闭

`Schedule.enabled` 默认改为 `false`，也就是装完按总开关**全天监测**，
要限制时段需要用户主动打开。

### 5. 权限引导

* 顶部先给一句总结：「还差 N 项必需权限」或「四项权限都已就绪」。
* 每张卡标明第几步（必需的两项是「第 1 步 / 第 2 步」），并直接写**怎么点**，
  不再只讲"为什么需要"。
* 主按钮直接指向**第一个还没授予的必需权限**，不用自己在四张卡里找。
* **从系统设置返回时自动刷新**（`LifecycleResumeEffect`）——
  之前授完权回来还是显示"未授予"，让人以为没生效。

### 6. 长标题把数值挤成一列竖排字

「休息默认时长（通知按钮 / 首页按钮用）」这类长标题会把右侧数值挤到没宽度。
现在标题 `weight(1f)` 占剩余空间、数值单行不换行；标题本身也缩短成「休息默认时长」，
括号里的说明挪到滑杆下方作为副标题。

### 另外

* 版本号 1.0.1（2），方便在「关于」页确认装的是哪一版。


## 1.0.0 — 首个正式发布

### 品牌：正式定名「知止」

* 名字取自《道德经·第四十四章》：“知足不辱，**知止不殆**，可以长久。”
  释义：知道适可而止，就不会遇到危险。
* `applicationId` 由 `app.zhizhi` 承接（namespace、DataStore 文件名、通知通道 id、
  logcat tag、悬浮窗标题、action 常量全部同步改完）。
  ⚠️ **包名一变等于一个全新的应用**：旧版不会自动升级，需要卸载重装，应用内数据不迁移。
  发布前改掉是最便宜的时刻——一旦上架就再也改不动了。
* 弹窗文案统一为知止句式：

  | 弹窗 | 文案 |
  | --- | --- |
  | 娱乐类提醒 | **知止不殆。你进来时想做什么？** |
  | 游戏前确认 | 知止不殆 · 即将入局 |
  | 休息结束 | 知止不殆 · 休息有度 |

* logcat 过滤命令变成 `adb logcat -s ZhiZhi`。

### UI 按设计稿重做

* 设计令牌换成 **indigo-600 主色 + slate 中性色**，卡片为白底 + 1px slate-200 描边 + 圆角 12。
* **底部三个 tab：首页 / 记录 / 设置**。
* 首页改为仪表盘：日期 → 「知止不殆」→ 主控卡片 → **三列统计卡**（今日提醒 / 拦回 / 监测，
  前两项带“较昨日 ±N”）→ **功能模块 2 列网格** → 《道德经》箴言。
* 子页面统一「圆形返回按钮 + 标题 + 分隔线」的顶栏；分组卡片统一「标题 + 分隔线 + 内容行」。
* 滑杆改为「标题与数值同一行，滑杆在下面」。
* 手写 13 个矢量图标，**没有引入 material-icons-extended**——APK 只涨约 20 KB。
* 悬浮提醒卡片同步换配色（白底 + 描边 + indigo 主按钮）。
* 统计的涨跌颜色带上产品判断：**提醒次数下降显示为绿（好事），上升为橙**。

### 发布：正式签名

* 生成 `zhizhi-release.jks`（RSA 4096，别名 `zhizhi`，有效期约 30 年）。
* release 包不再回落到 debug 签名；`keystore.properties` 与 `*.jks` 都在 `.gitignore` 里。
* 版本号从 **1.0.0 / versionCode 1** 重新起算（之前的 0.1.x 只是内部迭代）。

### 构建产物已验证

| 项目 | 结果 |
| --- | --- |
| 包名 / 版本 | `app.zhizhi` · 1.0.0（1） |
| 应用名 | 知止 |
| 体积 | 1.47 MB（R8 混淆 + 资源压缩） |
| 权限 | 6 项，**`INTERNET` 出现 0 次** |
| debuggable / testOnly | 无 |
| 签名 | `CN=ZhiZhi, OU=App, O=ZhiZhi, L=Beijing, ST=Beijing, C=CN` |

### 尚未验证

UI 是照设计稿重写的，**只经过编译验证，没有在设备或模拟器上看过实际效果**——
这台机器上跑 Android 模拟器不现实。首次安装后建议按 `docs/功能总览与测试案例.md`
走一遍，重点看首页仪表盘和各页卡片的间距。


## 0.1.7 — 未发布

### 撤销 0.1.6 里的 20 秒冷却下限（判断错了）

0.1.6 看到真机日志里"点了算了 → 0.4 秒后又弹"的现象，当成连珠炮，加了一道不可配置的 20 秒下限。

**这个判断是错的。** 那段日志里反复弹的根本原因是**用户在测试**——他反复重新打开原神，
每次都弹出来正是他想要的、并且满意的行为。我用一道冷却去"修"它，
等于拿掉了他要的功能。

**真正的病根在别处**：回答之后在 `onAnswer()` 里写了 `session = null`。
用户点"算了"到一键回桌面生效之间有 1.5 秒，这期间前台仍然是那个游戏，
于是下一 tick 立刻建出一个**新会话**——新会话的 `gamePrompted` 是 false，
所以 471 毫秒后又问了一遍。是"会话被过早销毁"，不是"缺少冷却"。

现在：
* `GAME_CANCEL` / `DONE_EXIT` / `DISTRACTED` / `MUTE_APP` **都不再清 session**。
  同一次停留里不重复问由会话自身保证（游戏的 `gamePrompted`，娱乐类的计数清零 + 重复间隔），
  这本来就是 session 存在的意义。
* 会话自然结束于"用户真的离开这个应用"，于是**重新打开 = 新会话 = 照常弹出确认**，
  反复重进每次都会问。测试想要的正是这个。
* 20 秒硬下限删除。

### 「回答之后的冷却」改名并重新定义

它现在管的是**离开再重新进入**，不是同一次停留：

* 「**重新进入该应用时的冷却**」，默认 **0 = 每次重新打开都会照常提醒**。
* 设成 30 秒之类的值，用途变成"别我一出去一进来就立刻问我"。
* 0.1.6 里这个滑杆的说明文案是错的（它声称能控制同一次停留的追问），顺手改正。

### 教训

一条现象在日志里成立，不等于它在用户那里是个问题。
**在拿掉一个明显是用户主动制造的行为之前，先问一句"这是不是你故意的"。**

## 0.1.6 — 未发布

### 默认值调整（按真机使用反馈）

| 设置 | 旧默认 | 新默认 |
| --- | --- | --- |
| 回答之后的冷却时间 | 120 秒 | **0（不额外冷却）** |
| 提醒卡片位置 | 屏幕底部 | **屏幕顶部** |
| 休息时长 | 10 分钟 | **15 分钟** |
| 进入游戏前先确认一次 | 开 | 开（不变） |

### 新增：回答后冷却的 20 秒硬下限

把冷却滑杆拉到 0 之后，真机出现了连珠炮：用户点"算了"→ 被送回桌面 → 游戏自己又回到前台
→ **0.4 秒后又被问一遍**。连着弹了五六次。

现在策略里有一道**不可配置**的 20 秒下限：`cooldown = max(用户设置, 20 秒)`。
理由很直接——"刚回答完 20 秒内再问一次"在任何场景下都不可能有意义，所以它不是偏好而是正确性。
滑杆控制的是下限之上的额外冷却。

### 修复：ReminderPolicy 的并发访问

主循环跑在 `Dispatchers.Default`，而卡片按钮点击、通知动作回调跑在主线程，
两边同时读写 `session` / `cardShowing` / `suppressed`。加锁之前存在丢失更新的可能，
表现正是"回答了但冷却没生效"。`onTick` / `onAnswer` / `reset` 现在都加了 `@Synchronized`。

### 新增：休息接管"暂停"的位置

* 通知里的「暂停 30 分钟」改成「**休息 N 分钟**」（N = 休息默认时长）。
  已休息时按钮变成「结束休息」，已暂停时变成「恢复监测」。
* **休息期间完全不监测、不提醒**（以前只有"暂停"会拦住监测）。
* 休息到点 → 弹卡片「休息结束 / **休息时间过了，请继续努力哦**」，
  两个按钮：「好的，现在开始」和「**再休息 N 分钟**」（N 可在设置里改）。
* 到点后立刻清掉休息状态并恢复监测，所以**无视卡片也不会被永久挡住**。
* 落点是 `breakEndsAtMs` 一个字段，不再分别维护"暂停"和"休息"两套时间线。
  纯粹的"安静停一会儿、到期不叫你"保留为「暂时停止监测」，挪进了其他个性化设置。

### 新增：其他个性化设置页

首页只留每天都会碰的东西（总开关、休息、学习时段、提醒阈值、入口行）。
搬过去的有：重复提醒间隔、回答后冷却（含说明）、冷静期、休息默认时长、再休息时长、
进入游戏前确认、卡片位置、开机自启、退出后清后台、暂时停止监测。

起因写在代码注释里了：0.1.4 之前"回答后的冷却"是写死的常量、界面上看不到，被当成 bug 排查了很久。
**会显著改变行为的参数，必须在界面上有一席之地**——而"有一席之地"和"堆在首页"是两件事。

### 新增：返回栈 + 系统返回

之前应用没有处理返回键，子页面按返回会直接退出。现在有真正的返回栈，
子页面返回上一级，首页才把返回交还给系统。

在 ColorOS 的手势导航下，**屏幕左右边缘向内滑本身就是系统的"返回"手势**，
它派发 `KEYCODE_BACK` —— 所以左滑/右滑返回上一级现在天然可用，不需要自己写手势识别
（自己写反而会和系统的边缘手势打架，也会和页面里的滑杆抢事件）。

### 新增：退出后清目标应用的后台（版本自适应）

点「已完成，退出」之后可选地顺手清掉那个应用的后台进程。

⚠️ **Android 14 起这条路被系统封死了。** 官方文档（`ActivityManager.killBackgroundProcesses`）原文：
"On devices that run Android 14 or higher, third party applications can only use this API to kill
their own processes." 传别人的包名只会得到一句 `Invalid packageName: ...`。
能杀别的应用的是 `KILL_ALL_BACKGROUND_PROCESSES`，保护级别 `privileged|signature`。

所以做成版本自适应：Android 13 及以下真的调用（生效），14+ 在界面上标成"本机不支持"并**禁用开关**。
不给用户一个永远不生效却看不出来的开关。

### 改进：卡片高度减半

五个按钮竖排时卡片窗口高 1080px ≈ 屏幕高度的 45%——对一个号称"不打扰"的工具太霸道。
改成"主按钮通栏 + 2×2 网格"，窗口高度降到约 600–700px，五个动作一个不少。

### 改进：解锁后立刻恢复监测

真机日志里出现过：解锁后 **17 秒**才恢复监测。原因是 `refreshScreenState()` 有 5 秒节流，
而屏幕广播正好落在节流窗口里被吞掉，之后主循环又要等 20 秒的 `SLEEP_TICK` 才醒。
现在广播路径会**强制**重算，不受节流约束。

## 0.1.5 — 未发布

### 新增：提醒卡片第五个按钮「别再提醒这个应用」

卡片上多一个刻意做得更轻的按钮（无底色、字更小），点一下把当前应用**永久**加入忽略名单：

* 不是一个更长的冷却，而是 `Long.MAX_VALUE` 抑制 + `AppCategory.IGNORED`，
  之后连会话都不会为它建立。
* **不会**把用户送回桌面——他说的是"这个应用我不管了"，不是"我要离开"。
* 降级通知里同样带上这个动作（放在前三位，因为折叠时只显示三个）。
* 「本地记录」页新增计数。

背景：预设名单里的微信/QQ 会被当成"娱乐/社交"监控，而用户很可能真的在回工作消息。
与其替他决定，不如把"这个应用别管了"做成一键操作。

### 修复：回答后的冷却时间是写死的，而且界面上看不到

真机上被误判成"同一个应用第二次就不弹了"的 bug，实际原因是：

* 回答"退出/走神"后该应用冷却 **10 分钟**
* 游戏取消后冷却 **5 分钟**

这些值既没暴露在界面上，也没写在文档里，用户只能靠反复测试猜。现在：

* 新增设置项 `answerCooldownSec`，首页「提醒节奏 → 回答之后的冷却时间」滑杆，0–10 分钟，
  **默认 2 分钟**，0 = 不冷却。
* 游戏前确认的间隔也改由同一个值控制，不再有第二套写死的常量。
* 三个写死的常量（`EXIT_SUPPRESS_MS` / `GAME_CANCEL_SUPPRESS_MS` / `GAME_PROMPT_COOLDOWN_MS`）全部删除。

**教训：一个会显著改变行为的策略参数，必须在界面上有对应开关。**

### 修复：自检卡片的自动移除会顺手收走别人的卡片

自检卡片排了 8 秒后自动 `hideCard()`。如果这 8 秒内用户拿到了一张**真**的提醒卡片
（真机日志里正是如此：原神确认卡片 10:46:16 出现，10:46:17 被自检的定时器移除），
那张真卡片会跟着消失，而策略仍以为它挂着。现在自动移除会校验卡片代次，被新卡片取代就跳过。

### 改进：诊断事件上限 60 → 1000，并且收拢成单独一个模块

* 内存保留 **1000 条**（权威层）。
* 落盘节流：每 2 秒最多写一次，且磁盘只存最近 **200 条**——
  1000 条 JSON 约 150 KB，每条记录都重写一遍纯属浪费 I/O。重启后可见的历史是 200 条。
* 诊断页把全部记录收进**一张卡片里的一块可选文本**（默认收起，点「展开」查看），
  不再是几百个独立条目各选各的。「复制完整诊断」导出全部 1000 条。

## 0.1.4 — 未发布

### 根因修复：卡片窗口的所有操作都改到主线程

真机诊断记录一次性暴露了从 0.1.0 起就存在的根因：

```
[Card] showCard -> FAILED
[Overlay] 卡片添加失败 RuntimeException: Can't create handler inside thread
         Thread[DefaultDispatcher-worker-7,5,main] that has not called Looper.prepare()
```

`WindowManager.addView()` 内部要构造 `ViewRootImpl`，而它会创建一个绑定**当前线程** Looper 的
`Handler`。监测主循环跑在 `Dispatchers.Default` 上，那里没有 `Looper.prepare()`，
所以**从后台弹卡片 100% 抛异常**。

这个 bug 藏了四个版本，因为它伪装得极好：Service 的生命周期回调本来就在主线程，
所以「应用内点测试卡片」永远成功；只有主循环那条路径失败。于是所有现象都指向
"厂商 ROM 拦截了后台悬浮窗"，而实际上**根本没有任何拦截，是线程用错了**。

* `OverlayController` 的公开 API 全部改为 **suspend**，内部
  `withContext(Dispatchers.Main.immediate)` 切主线程；调用方从任何线程调用都安全。
* 少数必须在主线程**同步**完成的地方（`startForeground` 之前要先挂好锚点窗口，
  这个顺序不能交给协程调度）走 `*OnMainThread()` 系列，它们是 internal 的，
  并带一次运行时断言——再有人搬到工作线程上，会直接出现在诊断记录里。
* `MonitorService` 的 `endSession()` / `execute()` / `presentCard()` 相应改为 suspend；
  `handleAnswer()` 保持在主线程可同步调用，把界面动作丢进协程执行。
* 卡片添加日志现在记录线程名，下次同类问题一眼可见。

### 同时修掉的两个策略问题

* **卡片失败后策略仍以为卡片挂着。** `cardShowing` 在 `presentCard` 失败/探测超时后没有回滚，
  于是整段会话都不再提醒——而用户其实什么都没看到。新增 `ReminderPolicy.onCardUnavailable()`。
* **游戏前确认会连续追问。** 用户选"算了"之后会话被清空，而他还在游戏里，立刻就会建出新会话，
  新会话的 `gamePrompted` 是 false → 马上再问一遍（真机日志里 0 毫秒后又弹了一次）。
  现在 GAME 分支与娱乐分支一样受 `nextNudgeAtMs` 约束，并带 5 分钟的确认冷却。

## 0.1.3 — 未发布

### 修复：诊断本身不允许静默失败

0.1.2 在真机上出现了最难查的情况：**代码确实调了 `record()`，但诊断页一条都读不到，
而且没有任何报错。** 那条「改用通知提醒你」的通知就在 `record()` 的下一行，
所以记录调用一定发生过 —— 说明问题出在我把诊断做成了一个会静默失败的东西。

* 诊断改成两层：**内存 StateFlow 是权威**（只要有调用就一定出现在 UI 上），
  DataStore 降级为"尽力而为"的持久化，失败只写 `lastError` 并打 Log.e，绝不影响上层。
* 新增**活体指标**：主循环心跳 + 当前判定。这两个值**完全绕开磁盘**，
  服务在跑就一定会动。这是"诊断到底有没有在撒谎"的判据。
* 新增「本进程累计记录」计数：只增不减。它涨、列表空 → 是读取端的问题，不是没记录。
* 新增「复制完整诊断到剪贴板」：反馈问题时直接粘贴，不需要 adb，也不需要截图。
* 诊断页整页改为单个滚动 Column，去掉 `weight(1f)`——
  原写法在内容超出屏高时会把事件列表压成 0 高度，看起来同样是"什么都没有"。
* 页面显示**包名**。一眼就能分辨你打开的是 `app.zhizhi` 还是 `app.zhizhi.debug`
  （两个是独立安装，各自的权限和数据互不相通）。

### 新增：把失败原因写进通知

降级通知现在多一行副标题 `原因：…`，取值是这四种之一：

* `悬浮窗权限对该应用未生效`
* `系统未提供 WindowManager`
* `addView 抛异常 <类名>`
* `卡片已加入窗口但始终不可见（attached=… visibility=… size=…）`

动机很直接：**通知是唯一被证实一定能到达用户的界面**。那就不要把原因只写在日志里，
把它写在用户一定会看到的地方。

### 改进

* 诊断页新增「一票否决开关」清单：悬浮窗权限、通知横幅、后台弹出界面、
  以及"不要在游戏空间/免打扰/省电模式下测试"。
* 降级通知文案改为中性的"提醒卡片没显示出来"——原来说"被系统拦截"其实是猜测，
  而在确认原因之前不该把猜测写进用户可见的文案。

### 仍未解决

**在 ColorOS 上，卡片为什么叠不到别的应用上面去，还没有定位。**
两个候选失效模式（`addView` 被拒 / 加入了但不可见）现在能靠通知副标题一次分清，
定位之前不做投机性的修改。

## 0.1.2 — 未发布

### 修复（全部来自 OPPO Find X9 Pro / Android 16 / ColorOS 的真机测试）

* **降级通知弹不出横幅。** 给通知调了 `setSilent(true)`，而官方文档明确写着它会
  "prevent the notification from peeking on screen, regardless of the importance... set on
  the notification or notification channel"。结果就是提醒只会变成通知栏里一条静默记录
  （国产 ROM 还会把它折进「静默通知」分组，看起来只有一个图标）。
  现在改为**通道无声、通知不静音**，横幅能弹但不会响。
* **卡片可见性探测太短，把慢布局误判成被拦截。** 原来只等 700ms 探测一次；
  `addView()` 之后窗口还没走完第一帧布局时 `width/height` 都是 0，设备一忙（比如正在启动游戏）
  就会被误判成"不可见"，白白降级成通知。现在改成 0/350/800/1600/2600ms 阶梯探测，
  任意一次通过即算成功。这正是"提醒时灵时不灵"的来源之一。
* **屏幕状态可能永久卡死。** 原来只靠 `ACTION_USER_PRESENT` 广播判定"已解锁"，
  某次广播没收到 `screenInteractive` 就永远停在 false，监测静悄悄地彻底停摆。
  现在每 5 秒与 `PowerManager.isInteractive()` + `KeyguardManager.isKeyguardLocked()`
  重新对齐一次，广播只是优化而不是唯一真相。
* **每次轮询都做一次 PackageManager 跨进程调用。** 判定"这个包是不是桌面"原本每个 tick
  都调一次 `queryIntentActivities`，既费电又会把轮询周期拉长。现在缓存一次。

### 改进

* **时段内轮询间隔 2.5 秒 → 1 秒**，把"打开游戏到弹确认"的总延迟从最坏 4 秒以上压到约 2 秒。
  时段外仍是 20 秒。
* 游戏前确认选"算了"之后，5 分钟内不再为同一个游戏弹确认（刚拒绝过就不该再问）。
* 服务声明 `android:stopWithTask="false"`，避免划掉最近任务时被连带停止。
* 新增 `[Loop]` 状态跳变日志：把"当时前台是哪个应用、被判成哪一类、是不是因为不在时段/
  没解锁而根本没在监测"直接写进诊断记录。这是排查"时灵时不灵"最关键的一条。
* 界面停止服务时会先记一条 `界面请求停止监测`，否则日志里只有一个孤零零的 onDestroy，
  分不清是用户操作的还是系统杀的。
* 诊断页新增「提醒通道（横幅）」一行，并在通道被降级时给出显式警告——
  通道重要性只在创建时生效，之后只能由用户在系统设置里改回来。

## 0.1.1 — 未发布

### 修复

* **卡片可见性不再依赖动画。** 0.1.0 把卡片 root 的 alpha 从 0 动画到 1，一旦动画没跑
  （后台进程被压帧率等），卡片就永远停在 alpha=0：表现为"提醒触发了但屏幕上什么都没有，
  logcat 也没有报错"。现在 alpha 直接为 1，动画只是可见之后的修饰。
* 卡片窗口设了标题（`ZhiZhi 卡片` / `ZhiZhi 锚点`），
  `dumpsys window windows | grep -i zhizhi` 可以直接过滤。
* 顶部卡片增加刘海屏适配（`LAYOUT_IN_DISPLAY_CUTOUT_MODE_SHORT_EDGES`）。

### 新增

* **提醒降级机制。** `showCard()` 被拒（无权限 / 无 WindowManager / addView 抛异常），
  或添加后 700ms 回查发现窗口不可见，都自动改为发一条带同样选项的**横幅通知**。
  提醒不会再因为悬浮窗被 ROM 拦截而丢失。
* **应用内「诊断」页。** 环境状态（四项权限、服务是否在跑、锚点窗口是否可见）、
  一键「测试提醒卡片（前台）」自检、最近 60 条关键事件记录、adb 抓取命令。
  真机上排查"提醒不弹"不再需要连电脑。
* 所有关键路径都写日志，tag 统一为 `ZhiZhi`：`adb logcat -s ZhiZhi` 一条命令抓全。
* 本地记录新增「提醒卡片实际显示 / 降级为通知」计数，用来量化 ROM 的拦截程度。
* 通知通道拆成三条：`zhizhi_monitor`(MIN) / `zhizhi_alerts`(HIGH，静音降级通道) /
  `zhizhi_service_low`(LOW)。

### 已知限制

* 前台自检能弹、后台不弹，说明该 ROM 拦截了后台悬浮窗，应用层无法绕过，只能走降级通知。
* 仍未在真机上完整验证，见 `docs/真机验收清单.md` 的 J 组。

## 0.1.0 — 未发布

首个可构建版本。功能骨架完整，但**尚未在真机上验证**，见 `docs/真机验收清单.md`。

### 已有

* 学习时段（支持跨午夜），时段外完全不监测
* 分类：不监控 / 娱乐社交 / 游戏，默认全部不监控
* 预设中文本土应用名单，与本机已装列表求交集后一键套用
* 前台应用识别基于 `UsageStatsManager` 事件流，不使用无障碍服务
* 后台计时 + 自适应轮询（1s / 2.5s / 20s）
* 分级提醒：娱乐类超时温和提醒；游戏类启动前确认一次，局内静默
* 提醒卡片四选项：已完成退出 / 再给 N 分钟 / 我走神了退出 / 我正在做正事
* 回答退出后的 10 分钟抑制窗口，避免工具本身变成新的干扰源
* 休息计时器
* 前台服务（`specialUse`）+ 1×1 隐形锚点窗口，满足 Android 15 的后台启动豁免要求
* 无声、无振动、`IMPORTANCE_MIN` 的通知通道
* 权限引导（含国产 ROM 后台设置跳转）
* 本地统计：只记次数与总时长，120 天后自动清理
* 「一键回到桌面」的实测探针，成功/失败计数写进本地统计
* 无 `INTERNET` 权限；CI 会检查这一点，并让构建失败

### 尚未做

* 真机验证（阶段 1 的三条前提全部待验证）
* 截图（`fastlane/.../phoneScreenshots/`）
* 正式签名密钥
* 电量实测数据
